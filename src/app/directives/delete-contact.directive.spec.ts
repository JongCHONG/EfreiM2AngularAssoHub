import { DeleteContactDirective } from './delete-contact.directive';

describe('DeleteContactDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteContactDirective();
    expect(directive).toBeTruthy();
  });
});
